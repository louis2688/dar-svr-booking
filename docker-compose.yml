services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: svr_booking
      POSTGRES_USER: svr
      POSTGRES_PASSWORD: svr
    ports:
      - "5432:5432"
    volumes:
      - svr_booking_pg:/var/lib/postgresql/data
volumes:
  svr_booking_pg:

